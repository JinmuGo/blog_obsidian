name: Trigger Blog Deploy

on:
  push:
    branches: [main]

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger jinmu.me deploy
        uses: peter-evans/repository-dispatch@v4
        with:
          token: ${{ secrets.TOKEN }}
          repository: JinmuGo/jinmu.me
          event-type: content-update
